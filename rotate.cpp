#include <iostream>
using namespace std;

int main()
{
    int s = nums.size();
     int arr[] = {1, 2, 3, 4, 5, 6, 7};

    int k = 3;

    //  for (int i = nums.size(); K = 0; i--)
    //  {
    //     nums.insert(i,arr[s])
    //     k--;
    //  }

    for (int i = 0; k < 0; i++)
    {
        while (k < 0)
        {
            nums.insert(i, nums[i])
            k--;
        }
    }

    return 0;
}